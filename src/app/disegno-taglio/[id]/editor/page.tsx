import CuttingSimulatorClient from "@/components/cutting-simulator/CuttingSimulatorClient";

export const metadata = {
    title: "Editor 3D Progetto | Magazzino V5.5",
    description: "Editor professionale per la progettazione del tracciato e l'esplosione dei tagli",
};

export default function EditorPage() {
    return <CuttingSimulatorClient />;
}
